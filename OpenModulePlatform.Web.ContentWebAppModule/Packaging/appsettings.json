{
  "Portal": {
    "SecurityHeaders": {
      "ContentSecurityPolicy": {
        "//": "Must stay identical to Portal:SecurityHeaders:ContentSecurityPolicy:Policy in OpenModulePlatform.Web.ContentWebAppModule/appsettings.json. The checked-in appsettings.json never reaches a host: the payload strips it and HostAgent writes this file instead, so a policy missing here silently falls back to the shared baseline. See docs/CONTENT_SECURITY_POLICY.md.",
        "Policy": "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' ws: wss:; media-src 'self'; worker-src 'none'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-src 'self'; frame-ancestors 'self'"
      }
    }
  },
  "ContentWebAppModule": {
    "AppInstanceId": "{{Omp.Json.AppInstanceId}}",
    "HomeSlug": "home",
    "ServerReportsPath": "App_Data/ContentReports",
    "HtmlFilesPath": "App_Data/ContentPages",
    "AllowedServerReportDatabases": [ "{{Omp.Json.ConnectionStrings.OmpDb.DatabaseName}}" ],
    "ServerReportDefaultMaxRows": 100,
    "ServerReportMaxRowsLimit": 1000,
    "ServerReportQueryTimeoutSeconds": 30
  },
  "NLog": {
    "autoReload": true,
    "throwConfigExceptions": true,
    "variables": {
      "appName": "OpenModulePlatform.Web.ContentWebAppModule",
      "logDirectory": "${basedir}/logs"
    },
    "targets": {
      "logfile": {
        "type": "File",
        "fileName": "${var:logDirectory}/${var:appName}-${shortdate}.log",
        "layout": "${longdate}|${uppercase:${level}}|${logger}|${scopeproperty:item=CorrelationId}|${message}${onexception:inner= ${exception:format=tostring}}"
      },
      "console": {
        "type": "Console",
        "layout": "${longdate}|${uppercase:${level}}|${logger}|${scopeproperty:item=CorrelationId}|${message}${onexception:inner= ${exception:format=tostring}}"
      }
    },
    "rules": [
      {
        "logger": "Microsoft.Hosting.Lifetime",
        "minLevel": "Info",
        "writeTo": "console,logfile",
        "final": true
      },
      {
        "logger": "Microsoft.*",
        "maxLevel": "Info",
        "final": true
      },
      {
        "logger": "*",
        "minLevel": "Info",
        "writeTo": "console,logfile"
      }
    ]
  }
}
