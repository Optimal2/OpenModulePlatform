@* File: OpenModulePlatform.Web.ContentWebAppModule/Pages/Page.cshtml *@
@page "{*slug:nonfile}"
@model OpenModulePlatform.Web.ContentWebAppModule.Pages.PageModel

@if (Model.PageContent is null)
{
    <section class="card content-empty">
        <h1>@L["Page not found"]</h1>
        <p class="muted">@L["The requested content page is disabled, missing, or not available for your active role."]</p>
    </section>
}
else
{
    <article class="content-page">
        <header class="content-page__header">
            <h1>@Model.PageContent.Title</h1>
        </header>
        <div class="content-page__body">
            @Html.Raw(Model.RenderedHtml)
        </div>
    </article>
}
