@page
@model OpenModulePlatform.Portal.Pages.Admin.HostDeploymentsModel

<section class="page-header">
    <div>
        <div class="eyebrow">@L["Automation"]</div>
        <h2>@L["HostDeployments.Title"]</h2>
        <p class="lede">@L["HostDeployments.Lede"]</p>
    </div>
</section>

@if (Model.Rows.Count == 0)
{
    <div class="empty-state">@L["No host deployments found."]</div>
}
else
{
    <table class="grid">
        <thead>
            <tr>
                <th>@L["Host key"]</th>
                <th>@L["Host template"]</th>
                <th>@L["Status"]</th>
                <th>@L["Requested by"]</th>
                <th>@L["Requested UTC"]</th>
                <th>@L["Completed UTC"]</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var row in Model.Rows)
        {
            <tr>
                <td><code>@row.HostKey</code></td>
                <td><code>@row.HostTemplateKey</code></td>
                <td><span class="pill">@row.Status.ToString()</span></td>
                <td>@row.RequestedBy</td>
                <td>@row.RequestedUtc.ToString("u")</td>
                <td>@row.CompletedUtc?.ToString("u")</td>
            </tr>
        }
        </tbody>
    </table>
}
