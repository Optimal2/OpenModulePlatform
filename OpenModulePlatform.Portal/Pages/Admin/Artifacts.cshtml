@page
@model OpenModulePlatform.Portal.Pages.Admin.ArtifactsModel

<section class="page-header">
    <div>
        <div class="eyebrow">@L["Definitions"]</div>
        <h2>@L["Artifacts"]</h2>
        <p class="lede">
            Artifacts are deployable builds for app definitions. App instances select
            which artifact version to use at runtime.
        </p>
    </div>
</section>

<div class="toolbar top-gap">
    <a class="action-link" href="~/admin/artifactedit">@L["Create artifact"]</a>
</div>

@if (Model.Rows.Count == 0)
{
    <div class="empty-state">@L["No artifacts found."]</div>
}
else
{
    <table class="grid">
        <thead>
            <tr>
                <th>@L["App key"]</th>
                <th>@L["Version"]</th>
                <th>@L["Package type"]</th>
                <th>@L["Target"]</th>
                <th>@L["Relative path"]</th>
                <th>@L["Enabled"]</th>
                            <th>@L["Actions"]</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var row in Model.Rows)
        {
            <tr>
                <td><code>@row.AppKey</code></td>
                <td>@row.Version</td>
                <td>@row.PackageType</td>
                <td>@row.TargetName</td>
                <td><code>@row.RelativePath</code></td>
                <td><span class="pill">@(row.IsEnabled ? L["Enabled"] : L["Disabled"])</span></td>
                <td><a class="action-link" href="~/admin/artifactedit?id=@row.ArtifactId">@L["Edit"]</a></td>
            </tr>
        }
        </tbody>
    </table>
}
