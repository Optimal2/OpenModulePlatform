# OpenModulePlatform.Web.iFrameWebAppModule

Proof-of-concept Razor module for OpenModulePlatform that renders the shared OMP top bar, three URL selector buttons, and an iframe whose source is resolved from `omp_iframe_module.urls`.
