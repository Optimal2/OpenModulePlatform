#!/bin/sh
# OMP pre-push hook wrapper (Git Bash / MSYS).
# The real implementation lives in pre-push.ps1 and must be PowerShell 5.1-safe.
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
exec powershell.exe -NoProfile -ExecutionPolicy RemoteSigned -File "$SCRIPT_DIR/pre-push.ps1"
